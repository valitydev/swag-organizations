description: Сотрудник организации
type: object
required:
  - id
  - userEmail
  - roles
properties:
  id:
    $ref: '#/parameters/userId.yaml'
  userEmail:
    $ref: '#/schemas/UserEmail.yaml'
  roles:
    $ref: '#/schemas/MemberRoleSet.yaml'
