type: object
required:
  - status
discriminator:
  propertyName: status
  mapping:
    'Pending': '#/components/schemas/InvitationPending.yaml'
    'Accepted': '#/components/schemas/InvitationAccepted.yaml'
    'Expired': '#/components/schemas/InvitationExpired.yaml'
    'Revoked': '#/components/schemas/InvitationRevoked.yaml'
properties:
  status:
    description: Статус приглашения
    readOnly: true
