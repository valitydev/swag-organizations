type: object
required:
  - status
discriminator:
  propertyName: status
  mapping:
    'Pending': '#/schemas/InvitationPending.yaml'
    'Accepted': '#/schemas/InvitationAccepted.yaml'
    'Expired': '#/schemas/InvitationExpired.yaml'
    'Revoked': '#/schemas/InvitationRevoked.yaml'
properties:
  status:
    description: Статус приглашения
    readOnly: true
