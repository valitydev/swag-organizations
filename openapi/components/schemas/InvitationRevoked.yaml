allOf:
  - $ref: './Invitation.yaml'
  - type: object
    properties:
      revokedAt:
        description: Дата и время принятия приглашения
        type: string
        format: date-time
      reason:
        description: Причина отзыва приглашения
        type: string
        maxLength: 1000
